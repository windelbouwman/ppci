
module main;
import io;

function void main()
{
  var byte x;
  x = 22;
  io.print2("x=", x);
  x = x + 233;
  io.print2("x=", x);
  x = x + 2;
  io.print2("x=", x);
  x = x - 40;
  io.print2("x=", x);
}
